import { Button } from 'bootstrap';
import React, { Component } from 'react';

export default class cartAdd extends Component {
  render() {
    return (
      <Button color="success">Adicionar ao carrinho</Button>
    );
  }
}
